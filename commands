# Full UI (σ sliders, mesh panel, sandbox) — USE THIS:
python -m triangulation.server
http://localhost:5050/

# Public demo (Render — stays up when your PC is off):
# 1) Push main to GitHub
# 2) https://dashboard.render.com/ → New → Blueprint → this repo
# 3) Share the URL, e.g. https://tactical-edge.onrender.com/

# Static files only, NO API (legacy, optional):
# python3 -m http.server 8080
# http://localhost:8080/ui/index.html
#Cmd+Shift+R to refresh

# Offline triangulation (only if regenerating localizations.json):
# python -m triangulation.locate --in detection/output/events.json --out detection/output/localizations.json --pretty
